<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DataCollector.Views.PickingList.PickingSelectionPage">
    <ContentPage.Content>
        <StackLayout>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>                    
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0"
                   Grid.Column="0"
                   Text="Select Picking Choice:"/>
                <Picker Grid.Row="0"
                    Grid.Column="1"
                    Title="Pick"
                    ItemsSource="{Binding PickingDetailList}"
                    SelectedItem="{Binding SelectedPickingDetail}"/>


            </Grid>

            <Button Text="Select"
                    TextColor="White"
                    BackgroundColor="Black"
                    Command="{Binding SelectCommand}"
                    />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>